#include <stdio.h>
int main () {
    float a;
    float b;
    float c;
    float d;
    float x1;
    float x2;
    float n;

    scanf("%f %f %f", &a, &b, &c);
    d = b*b - 4*a*c;

    if (d<0); {
        n==0;
}
    if else (d==0); {
        n=1;
        x1=(-b/(2=a));
}
    if else (d>0); {
        n=2;
        x1=((-b)+sqrt(d))/(2*a);
        x2=((-b)+sqrt(d))/(2*a);
}
    printf("%f\n", x);

    return 0;
}



